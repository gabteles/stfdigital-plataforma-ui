declare namespace app.support.constants {
    /**
     * Propriedades compartilhadas da aplicação
     */
    interface Properties {
        url: string;
        port: number;
        apiUrl: string;
        development: boolean;
    }
}
